<!DOCTYPE html>
<html>
    <head>
        <title><%= title %></title>
        <meta charset="utf-8">
    </head>
    <body>
        <h1><%= title %></h1>

        <% if (emailsVisible) { %>
            <h3>Электронные адреса</h3>
            <ul>
                <% for (var i = 0; i < emails.length; i++) { %>
                    <li><%= emails[i] %></li>
                <% } %>
            </ul>
        <% } %>
    <p>Телефон: <%= phone %></p>
    </body>
</html>